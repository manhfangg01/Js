// KIỂU NGUYÊN THỦY
// number không phân biệt nguyên thưc
const n1 = 1;
const n2 = 1.0;

// string

const name = "manh"; // nên dùng
const surname = "phan";
const lop = `lop ${n1}`; // format string

// boolean

// undefined
let bla;
console.log(bla);

// xác định kiểu

console.log(typeof bla);

// ===================================
